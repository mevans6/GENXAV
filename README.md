# Gen X AV

Static marketing site. Open `index.html` in a browser or serve this directory with any static HTTP server.

The project intake form intentionally performs client-side validation only. See the TODO in `index.html` before connecting an approved backend.
